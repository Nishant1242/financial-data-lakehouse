name: 'lakehouse_dbt'
version: '1.0.0'
config-version: 2

profile: 'lakehouse_dbt'

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  lakehouse_dbt:
    staging:
      +materialized: view
    marts:
      +materialized: table